using System;
using System.Collections.Generic;
using System.Linq;
using System.Net.Http;
using System.Web.Http.Description;
using Newtonsoft.Json;
using Swashbuckle.Swagger;

namespace BookStoreApiService.SwaggerHelpers.Filters
{
    public class SwaggerGeneratorExt //: SwaggerGenerator
    {
        //private readonly SwaggerGenerator _sg;

        //public SwaggerGeneratorExt(ISwaggerProvider sg) 
        //{
        //    _sg = (SwaggerGenerator)sg;
        //}

        //public SwaggerGeneratorExt(IApiExplorer apiExplorer, JsonSerializerSettings jsonSerializerSettings, IDictionary<string, Info> apiVersions, SwaggerGeneratorOptions options = null) : base(apiExplorer, jsonSerializerSettings, apiVersions, options)
        //{
        //}
    }
}